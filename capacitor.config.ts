import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.angular1',
  appName: 'ionic-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
